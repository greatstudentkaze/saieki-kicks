import { NextPage } from 'next';

const OrderNotFoundPage: NextPage = () => {
    return (
        <main>
            <h1>Заказ не найден</h1>
        </main>
    );
};

export default OrderNotFoundPage;
